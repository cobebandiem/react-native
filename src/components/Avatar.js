import React from 'react';
import {
    View,
    Text
} from 'react-native';
function Avatar(props) {
    return (
        <View style={props}>
            
        </View>
    );
}

export default Avatar;